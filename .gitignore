# Secrets — nunca commitar
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer

# Dependências PHP
vendor/

# Vídeo do trailer (não commitar até ter o trailer oficial)
assets/video/trailer.mp4
public/assets/video/trailer.mp4

# Docker local (apenas para desenvolvimento)
compose.yml
docker-compose.override.yml

# Uploads de utilizadores (dados pessoais)
assets/avatars/
public/assets/avatars/

# Logs e caches
*.log
*.cache
/logs/
/storage/

# Sessões PHP
/sessions/

# Claude Code / tooling de agentes (local, não pertence ao projeto)
.claude/
.agents/
skills-lock.json

# IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
Thumbs.db
desktop.ini

# Backups e dumps de dados (apenas schema é seguro para commitar)
*_data_dump.sql
*_backup.sql
*_dump.sql

# Dump local da BD — pode vir a conter dados de utilizadores;
# o schema versionado vive em database/schema.sql
/sylora.sql

# Node e ferramentas de teste/browser
node_modules/
.playwright/
playwright-report/
test-results/

# Composer
composer.phar
